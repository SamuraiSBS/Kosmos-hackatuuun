# Космос Ферма

Мобильный web-MVP для КосмоХакатона. Пользователь исследует игровую карту сельскохозяйственной территории, находит аномалию на поле и принимает решение на основе спутниковых данных.

## Демонстрационный сценарий

1. Дождитесь завершения орбитальной заставки или нажмите «Пропустить».
2. Нажмите «Начать экспедицию».
3. Откройте подсвеченное поле A и выберите «Провести анализ».
4. Дождитесь завершения спутникового сканирования.
5. Выберите решение «Увеличить полив».
6. Дождитесь синхронизации и вернитесь на карту.

После успешного прохождения состояние хозяйства изменяется с 72% до 86%, а игрок получает 120 XP.

## Запуск

Требуется Node.js 18 или новее.

```bash
npm install
npm run dev
```

Откройте адрес, который выведет Vite, обычно `http://localhost:5173`.

## Команды

```bash
npm run dev      # запуск dev-сервера
npm run build    # проверка TypeScript и production-сборка
npm run lint     # проверка TypeScript
npm test         # запуск unit-тестов
npm run preview  # просмотр production-сборки
```

## Режим отладки

Добавьте `?debug=true` к адресу приложения:

```text
http://localhost:5173/?debug=true
```

В панели `DEV / DEBUG` доступны сброс прогресса, пропуск заставки, запуск и завершение миссии, изменение состояния поля и добавление XP.

Прогресс сохраняется в браузере в `localStorage` под ключом `kosmos-farm-mvp-state`. Для полного сброса можно использовать кнопку `Reset game` в режиме отладки.

## Технологии

- React + TypeScript
- Vite
- CSS без UI-фреймворка
- `useReducer` и Context API для состояния игры
- mock-данные спутникового анализа без внешнего API

Интерфейс спроектирован в mobile-first подходе с базовым размером экрана 390 × 844 px и адаптацией примерно от 320 до 500 px.

## Структура проекта

```text
src/
├── app/         # корневой компонент приложения
├── components/  # переиспользуемые UI и игровые компоненты
├── game/        # состояние игры, миссии, карта и mock-данные
├── scenes/      # сцены заставки, карты, анализа, решения и результата
├── services/    # граница интеграции со спутниковым API
└── styles/      # глобальные стили
```

## Текущие ограничения

- Спутниковые данные генерируются локально в `src/game/mockSatelliteData.ts`.
- Доступна одна миссия — аномалия на северном поле.
- Поля B и C, профиль и дополнительные миссии представлены как заготовки.
- Игровые ассеты реализованы временными CSS-фигурами и могут быть заменены на PNG, WebP или SVG без изменения игровой логики.

## Production-сборка

```bash
npm run build
npm run preview
```

Готовые файлы сборки будут размещены в директории `dist/`.
